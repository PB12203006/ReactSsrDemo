import React from 'react';

const Home = () => {
    return (
        <div>
            <p>abdc</p>
            <button onClick={() => alert('!')}>x</button>
        </div>
    );
};

export default {
    component: Home,
};
